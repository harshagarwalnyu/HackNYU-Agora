# Frontend (Next.js) public environment variables
# These are inlined at build time

# Base URL of backend (FastAPI) used by Socket.IO client
NEXT_PUBLIC_WS_URL=http://localhost:8000

# Base URL for REST endpoints (materials upload, health, etc.)
NEXT_PUBLIC_API_URL=http://localhost:8000
