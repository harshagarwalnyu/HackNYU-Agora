# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Socket.IO - CRITICAL: Keep version 5.10.0
python-socketio==5.10.0
python-engineio==4.9.0

# Configuration & Settings
python-dotenv==1.0.0
pydantic==2.9.2
pydantic-settings==2.4.0
attrs==23.2.0

# HTTP Client
httpx==0.26.0
aiofiles==23.2.1

# AI services
groq>=0.4.0
sentence-transformers==2.3.1
transformers==4.37.2
torch==2.1.2
torchvision==0.16.2
langgraph

# Vector DB
qdrant-client==1.7.3

# Audio Processing
edge-tts>=6.1.9
numpy<2
# Note: soundfile, numpy, scipy are deps of the above, pip will resolve them.
# Keeping them explicit if specific versions were important, otherwise relying on resolution.
# We'll let pip resolve them to avoid conflicts, or pin if needed.
# Converting dependencies to be minimal where possible.

# Logging
python-json-logger==2.0.7

# Search
lxml>=4.9.0
